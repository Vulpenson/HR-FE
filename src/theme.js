import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#0e2e4a', // your custom color
        },
        secondary: {
            main: '#5897c4', // another custom color
        },
        // You can define other colors and properties here
    },
});

export default theme;